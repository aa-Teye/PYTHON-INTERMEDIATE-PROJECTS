# Project 06: File Distiller
An automation tool that cleans up messy directories by sorting files into sub-folders based on their extensions.

## Features
- Scans directories for files.
- Automatically creates sub-folders (e.g., .pdf, .jpg).
- Uses standard Python libraries (no external install needed).
